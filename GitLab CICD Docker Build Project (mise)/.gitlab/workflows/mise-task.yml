.mise-task:
  image: docker:27
  services:
    - docker:27-dind
  before_script:
    - apk add --no-cache bash curl
    - curl -fsSL https://mise.run | sh
    - export PATH="${HOME}/.local/bin:${PATH}"
  script:
    - mise run "${MISE_TASK}"
  variables:
    DOCKER_TLS_CERTDIR: ""
